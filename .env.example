# Stellar Soroban Secret Key (S...)
# Generate with: soroban keys generate --seed <alias>
# Keep this SECRET - never commit or share
SOROBAN_SECRET_KEY=SBXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Optional: Your Soroban account ID (starts with 'G')
# If not set, will be derived from secret key
# SOROBAN_ACCOUNT_ID=GXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# =============================================================================
# RUNTIME GUARD DEPLOYMENT CONFIGURATION
# =============================================================================

# Soroban Network Configuration
# Options: testnet, futurenet, mainnet
SOROBAN_NETWORK=testnet
DEPLOYMENT_NETWORK=testnet
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
SOROBAN_RPC_TIMEOUT=30

# Deployment Configuration
VALIDATION_INTERVAL=300
MAX_RETRIES=3
VALIDATE_AFTER_DEPLOY=true
CONTINUOUS_VALIDATION=true

# Debug and Logging
DEBUG=false
LOG_LEVEL=info

# Advanced Options
DRY_RUN=false
DEPLOYMENT_TIMEOUT=30
SKIP_BUILD=false
COLLECT_METRICS=true

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in SOROBAN_SECRET_KEY with your S-prefixed key
# 3. Optionally add SOROBAN_ACCOUNT_ID if different from derived value
# 4. For GitHub Actions: Add SOROBAN_SECRET_KEY to repository secrets
# 5. Load locally: source .env.local
#
# NEVER COMMIT .env.local TO VERSION CONTROL
#
# =============================================================================

